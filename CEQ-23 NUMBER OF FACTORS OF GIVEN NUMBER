#include <stdio.h>

int main() {
    int n, i, factors = 0;

    printf("Enter a number: ");
    scanf("%d", &n);

    for(i = 1; i <= n; i++) {
        if(n % i == 0) {
            factors++;
        }
    }

    printf("The number of factors of %d is %d", n, factors);

    return 0;
}
